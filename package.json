{
  "name": "server_auto_browser",
  "version": "1.0.0",
  "description": "Server Auto Browser by SxS",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "start": "node --env-file-if-exists=.env --watch server.ts",
    "start:prod": "node --env-file-if-exists=.env server.ts",
    "build:cffsts": "tsc --noEmit && esbuild server.ts --bundle --packages=external --platform=node --format=esm --target=node23 --outfile=\"../../Delphi 7 Projects/CFFStS/ServerAutoBrowser/server.mjs\""
  },
  "author": "SxS",
  "license": "ISC",
  "engines": {
    "node": ">=23.6.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "puppeteer": "^24.40.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "esbuild": "^0.27.7",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
